/** Custom theme palette for footer */
export interface PaletteFooter {
  bg: string
}

/** Custom theme palette for cards */
export interface PaletteCard {
  bg: string
  header: string
  color: string
}

/** Custom theme palette for inline code and code blocks */
export interface PaletteCode {
  bg: string
  border: string
  color: string
}

/** Custom theme palette for transparent paper */
export interface PaletteTransparentPaper {
  bg: string
}
