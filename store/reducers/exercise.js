import defaultInitialState from '../defaultInitialState'

function exercise(state = defaultInitialState.exercise, action) {
  switch (action.type) {
    default:
      return state
  }
}

export default exercise
