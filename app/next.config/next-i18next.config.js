import path from 'path'

export default {
  i18n: {
    defaultLocale: 'de',
    localePath: path.resolve('.', 'src', 'public', 'locales'),
    locales: ['de', 'en'],
    // serializeConfig: false,
  },
}
