import { selectors } from '../slices/sections.js'

export const selectSection = (state) => {}

export const selectNextPrevSections = (state) => ({ prevId: '', nextId: '' })

export const selectBreadcrumbSections = (state) => []

export const selectSubsections = (state) => []

export const selectSectionTitle = (state) => []
