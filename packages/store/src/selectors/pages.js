import { createSelector } from '@reduxjs/toolkit'

export const selectPage = createSelector((state) => ({}))

export const selectNavPages = createSelector((state) => [])

export const selectFooterPages = createSelector((state) => [])
